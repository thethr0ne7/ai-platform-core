{
  "name": "@thr0ne7/ai-platform-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx src/server.ts",
    "check": "tsc --noEmit",
    "start": "node --enable-source-maps dist/server.js",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
